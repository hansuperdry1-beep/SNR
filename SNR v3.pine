def draw_stats():
    # Other code...
    text_size = size.normal
    # 5 more occurrences of text_size=size.small changed to text_size=size.normal
    # More code...  


def create_trade():
    dir_lb = size.normal  # Changed from size.small
    sl_lb = size.normal   # Changed from size.small
    tp_lb = size.normal   # Changed from size.small
    # Other code...